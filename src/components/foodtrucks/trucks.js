import React from 'react';

class Trucks extends React.Component {
  render() {
    return (
      <div className='lunch'>
      </div>
    );
  }
}

export default connect(store => {} , dispatch => bindActionCreators(, dispatch))(Trucks); 